node_modules
dist
coverage
.env
.env.local
*.tsbuildinfo

# macOS
.DS_Store

# Default STORAGE_LOCAL_ROOT for STORAGE_ADAPTER=local. Real uploaded files;
# never a build artifact, and never something to commit.
/storage/
