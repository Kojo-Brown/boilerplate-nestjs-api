# Refuse to install on a Node version outside `engines.node` instead of
# discovering the incompatibility at runtime. The CI matrix covers exactly the
# versions that range allows.
engine-strict=true

# Peer-dependency mismatches are warnings by default. CI treats warnings as
# failures, so fail at install time rather than printing something nobody reads.
strict-peer-dependencies=true
